namespace TCompiler.Types.CompilingTypes.Variable
{
    public class Uint : Variable
    {
        public Uint(bool isValue, string name = null, byte value = 0) : base(isValue, name, value)
        {
        }
    }
}