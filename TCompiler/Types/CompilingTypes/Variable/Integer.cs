namespace TCompiler.Types.CompilingTypes.Variable
{
    public class Integer : Variable
    {
        public Integer(bool isValue, string name = null, byte value = 0) : base(isValue, name, value)
        {
        }
    }
}